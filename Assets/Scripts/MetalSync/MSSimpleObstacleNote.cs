namespace MetalSync
{
    public class MSSimpleObstacleNote
    {
        public string identifier { get; private set; }

        public MSSimpleObstacleNote(string identifier)
        {
            this.identifier = identifier;
        }
    }
}